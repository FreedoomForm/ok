/**
 * Admin dashboard — shared constants.
 */

/** localStorage key prefix used for persisting dashboard UI state. */
export const DASHBOARD_UI_STORAGE_PREFIX = 'autofood:dashboard-ui'

/** Default (all-off) order filter flags — re-exported for convenience. */
export { DEFAULT_ORDER_FILTERS } from '@/components/admin/dashboard/types'
