Focused contracts universal Plus browser observation: after rebuilding with local DATABASE_URL, the universal create command opened the ContractsTab create form. The screenshot showed the new customer option selected, default dates populated, and the visible Russian “Сохранить” button rendered with enabled styling. Playwright’s `.last()` locator instead resolved to a disabled save button, indicating a duplicate matching control outside the visible form; the next test adjustment should scope Save to the ContractsTab/main create form rather than changing application behavior.
Cycle-51 full browser run exposed a real test-isolation issue in the new contract Edit proof: concurrent contract fixtures made `main input[type=checkbox].first()` select another fixture’s row, and the first unpaid button mutated that other contract. The page screenshot showed the wrong selected customer. Fix requires stable public resource-row identifiers and test locators scoped to the fixture contract, not retries or production behavior changes.
